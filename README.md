Generar los códigos HTML y CSS de 3 páginas Web con estas características (como mínimo esto, si agregan no hay problema, pero no pueden faltar estos):

Página 1
Nombre:  index.html
Armar una Web donde recomienden: Libros o Películas o Series (con uno de los tres tipos está bien pero con varias recomendaciones, por lo menos 8), aplicando:

Estructura completa de HTML5 (además de Doctype, meta, html, head, body, aplicar div y span, aplicar todos los que vimos semánticos para estructuras HTML5 como header, nav, article, section, footer, details, aside, etc.)
Un menú tipo nav que nos lleve a diferentes zonas dentro de la Web
Imágenes con textos alternativos
Links en imágenes. Estos links deben abrir en pestañas nuevas
Por lo menos un video desde un archivo local (adjunto en el rar o zip) con controles en pantalla
Por lo menos un video tipo iframe de Youtube
Por lo menos un audio (mostrando los controles)
La primera recomendación debe tener un link a la página 2 que pedimos mas abajo, abriendo en la misma pestaña actual.
Se debe incluir un link "Encuesta" que lleve a la página 3 que pedimos mas abajo, abriendo en una pestaña nueva.
En el pie de página deben estar los datos del alumno autor de sitio
Aplicar todos los estilos CSS que sean necesarios para que la Web quede lo mejor posible. Debe ser un archivo especifico para este HTML.
Página 2
Nombre:  desarrollo.html
Armar una Web relacionada con la Página 1 donde se desarrolle en detalle una de las series / noticias / libros de la Web principal.

Aplicar (por lo menos):

Estructura HTML completa (deben incluir doctype, meta, html, head, body, etc.)
Debe contener por lo menos un div y un span (más también)
Por lo menos una imagen con texto alternativo 
Por lo menos un video relacionado con el contenido desarrollado, ya sea archivo o iframe.
Tags para textos vistos en la materia
Párrafos y títulos
Una tabla (con mínimo 4 filas)
Listas ordenadas y sin orden
Links a sitios externos relacionados con el contenido que abran en ventanas nuevas
Link al sitio principal para volver a la página 1, este debe abrir en la misma ventana actual
Pie de página con los datos del alumno autor de sitio
Aplicar todos los estilos CSS que sean necesarios para que la Web quede lo mejor posible. Debe ser un archivo especifico para este HTML.

Página 3
Nombre:  encuesta.html

En esta Web los visitantes podrán responder una encuesta en relación a los temas desarrollados en la página 1:

Estructura HTML completa (deben incluir doctype, meta, html, head, body, etc.)
Aplicar todos los estilos CSS que sean necesarios para que la Web quede lo mejor posible. Debe ser un archivo especifico para este HTML.
Link al sitio principal para volver a la página 1, este debe abrir en la misma ventana actual
Pie de página con los datos del alumno autor de sitio
Crear por lo menos un formulario, agregando las etiquetas (labels) correspondientes,  aplicando como mínimo estos controles:
4 controles para ingreso de textos
3 con opciones tipo radio
3 con selecciones tipo checkbox 
1 combo de opciones tipo datalist (se recomienda colocar los libros/películas/series que están en la página 1)
1 texto largo tipo textarea
1 botón
